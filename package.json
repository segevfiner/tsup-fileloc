{
  "name": "tsup-fileloc",
  "version": "0.1.0",
  "private": true,
  "description": "tsup & esbuild-plugin-fileloc bug",
  "bin": "dist/index.js",
  "scripts": {
    "build": "tsup",
    "watch": "tsup --watch",
    "type-check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Segev Finer <segev208@gmail.com>",
  "license": "Unlicense",
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "@types/node": "^16.11.47",
    "esbuild-plugin-fileloc": "^0.0.6",
    "tsup": "^6.2.1",
    "typescript": "^4.7.4"
  }
}
